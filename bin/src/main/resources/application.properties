spring.mvc.view.prefix=/pages/
spring.mvc.view.suffix=.jsp
 
server.baseUrl=${BASE_URL:http://localhost:8080}
server.port=${PORT:8080}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${RDS_HOSTNAME:localhost}:${RDS_PORT:3306}/${RDS_DB_NAME:freshlandindia}
spring.datasource.username=${RDS_USER:root}
spring.datasource.password=${RDS_PASSWORD:shahid9970}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# Set the maximum allowed file size (in bytes)
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:500MB}
# Set the maximum allowed request size (in bytes)
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:500MB}

